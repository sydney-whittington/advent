--Day 2: Corruption Checksum

module Day2 where

import Data.List
--import Data.List.Split

--day2 = do
--    contents <- readFile "day2.txt"
--    let rows = lines contents
--        numbers = map (map read) $ map (splitAt "\t") rows
--    putStrLn (show numbers)
